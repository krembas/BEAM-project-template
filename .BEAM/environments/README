# This folder contains project environments subfolders that BEAM passes to ansible as its inventories during its runtime.
# Everything related to ansible inventories that is supported by ansible can be put here.
