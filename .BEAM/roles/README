# This folder contains all project-related (used for this project) ansible roles used by project jobs playbooks that BEAM passes to ansible during its runtime.
# Everything related to ansible roles that is supported by ansible can be put here.
